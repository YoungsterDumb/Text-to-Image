# Text to Image using TensorFlow

This project generates images from text descriptions using TensorFlow and TensorFlow Hub.

## Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/Text-to-Image.git
   cd Text-to-Image

2. Install the dependencies:
    ```bash
    pip install -r requirements.txt